<section class="posts" level="<%= level %>" timeline-id="<%= timeline.store.id %>" version="<%= timeline.store.version %>">
  <% timeline.fetch_all(level == 0 ? :upward : :downward).each do |post| %>
  <%=  erb :_post, :locals => { :post => post, :level => level } %>
  <% end %>
</section>
