<!doctype html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <title>ぽろったー</title>

    <%= erb :_head %>

    <% timeline = target.store.primary_timeline %>
    <script>
      $(function() {
        MyPage.fillPosts($('[timeline-id="<%= timeline.store.id %>"]'));
        startWatch(function(data) { updateTimeline(data.timeline, data.version); });
      });
    </script>

  </head>

  <body>
    <header>
      <h1>ぽろったー</h1>
      <p class="subtitle">ぽろったーは、リアルタイムに配信される情報にコメントを付けながら交流できるシステムです。</p>
    </header>

    <section>
      <h1><%= target.store.username %>さんの投稿</h1>
    </section>

    <div id="root">
      <section class="posts" timeline-id="<%= timeline.store.id %>" timeline-version="<%= timeline.store.version %>">
      </section>
    </div>
  </body>
</html>
